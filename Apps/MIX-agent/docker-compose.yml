# Generated by scripts/render_zimaos_compose.py; edit the generator.
name: mix-agent
services:
  init:
    image: ghcr.io/taitai2661/mix-agent-init:v0.2.0
    entrypoint:
    - python
    - /bootstrap.py
    network_mode: none
    volumes:
    - keys:/keys
    - db-secret:/db-secret
    - execution-token:/execution-token
    - mcp-token:/mcp-token
    - manager-token:/manager-token
    - browser-token:/browser-token
    - browser-provisioner-token:/browser-provisioner-token
    - browser-data:/browser-data
    - data:/data
    - policy:/policy
    - workspace:/workspace
    - packages:/packages
    - mcp-shared:/shared
  db:
    image: postgres:17.5-bookworm
    restart: unless-stopped
    environment:
      POSTGRES_DB: mix
      POSTGRES_USER: mix
      POSTGRES_PASSWORD_FILE: /db-secret/password
    volumes:
    - postgres:/var/lib/postgresql/data
    - db-secret:/db-secret:ro
    networks:
    - database
    depends_on:
      init:
        condition: service_completed_successfully
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U mix -d mix
      interval: 5s
      timeout: 3s
      retries: 20
  app:
    restart: unless-stopped
    read_only: true
    cap_drop: &id001
    - ALL
    security_opt: &id002
    - no-new-privileges:true
    pids_limit: 128
    tmpfs: &id003
    - /tmp:size=256m,mode=1777
    ports:
    - 8080:8080
    environment:
      DB_PASSWORD_FILE: /db-secret/password
      MIX_DATA: /data
      MIX_KEYS: /keys
      RUNNER_KEYS: /runner-keys
      PUBLIC_ORIGIN: ${PUBLIC_ORIGIN:-http://localhost:8080}
    volumes:
    - data:/data
    - policy:/data/egress
    - keys:/keys
    - db-secret:/db-secret:ro
    - execution-token:/execution-keys:ro
    - mcp-token:/mcp-keys:ro
    - manager-token:/manager-keys:ro
    - browser-token:/browser-keys:ro
    - browser-provisioner-token:/browser-provisioner-keys:ro
    command:
    - sh
    - -c
    - mkdir -p /tmp/runner-keys && cp /execution-keys/execution.token /mcp-keys/mcp.token /manager-keys/manager.token
      /browser-keys/browser.token /browser-provisioner-keys/browser-provisioner.token /tmp/runner-keys/ && export
      RUNNER_KEYS=/tmp/runner-keys && alembic upgrade head && uvicorn mix_agent.main:app --host 0.0.0.0 --port 8080
      --workers 1
    networks:
    - database
    - execution
    - mcp
    - internet
    extra_hosts:
    - host.docker.internal:host-gateway
    mem_limit: 1g
    cpus: 2
    depends_on:
      db:
        condition: service_healthy
    image: ghcr.io/taitai2661/mix-agent-app:v0.2.0
  execution-runner:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    environment:
      HTTP_PROXY: http://egress-proxy:3128
      HTTPS_PROXY: http://egress-proxy:3128
      http_proxy: http://egress-proxy:3128
      https_proxy: http://egress-proxy:3128
    volumes:
    - workspace:/workspace
    - execution-token:/tokens:ro
    networks:
    - execution
    mem_limit: 2g
    cpus: 2
    shm_size: 256m
    depends_on:
      init:
        condition: service_completed_successfully
    image: ghcr.io/taitai2661/mix-agent-execution:v0.2.0
  browser-runner:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    environment:
      HTTP_PROXY: http://egress-proxy:3128
      HTTPS_PROXY: http://egress-proxy:3128
      TOKEN_FILE: /tokens/browser.token
    volumes:
    - browser-data:/browser:ro
    - browser-token:/tokens:ro
    networks:
    - execution
    mem_limit: 2g
    cpus: 2
    shm_size: 256m
    depends_on:
      init:
        condition: service_completed_successfully
    image: ghcr.io/taitai2661/mix-agent-browser-runner:v0.2.0
  browser-provisioner:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    environment:
      HTTP_PROXY: http://egress-proxy:3128
      HTTPS_PROXY: http://egress-proxy:3128
      TOKEN_FILE: /tokens/browser-provisioner.token
    volumes:
    - browser-data:/browser
    - browser-provisioner-token:/tokens:ro
    networks:
    - execution
    mem_limit: 1g
    cpus: 1
    depends_on:
      init:
        condition: service_completed_successfully
    image: ghcr.io/taitai2661/mix-agent-browser-provisioner:v0.2.0
  mcp-runner:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    environment:
      HTTP_PROXY: http://egress-proxy:3128
      HTTPS_PROXY: http://egress-proxy:3128
      http_proxy: http://egress-proxy:3128
      https_proxy: http://egress-proxy:3128
      HOME: /packages
    volumes:
    - packages:/packages
    - mcp-shared:/shared
    - mcp-token:/tokens:ro
    networks:
    - mcp
    mem_limit: 512m
    cpus: 1
    depends_on:
      init:
        condition: service_completed_successfully
    image: ghcr.io/taitai2661/mix-agent-mcp:v0.2.0
  mcp-manager:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    image: ghcr.io/taitai2661/mix-agent-mcp-runtime:v0.2.0
    user: 1000:1000
    group_add:
    - '0'
    environment:
      TOKEN_FILE: /tokens/manager.token
      POLICY_FILE: /policy/mcp-policy.json
      MCP_RUNTIME_IMAGE: ghcr.io/taitai2661/mix-agent-mcp-runtime:v0.2.0
      MCP_EGRESS_CONTAINER: mix-agent-egress-proxy-1
    volumes:
    - /var/run/docker.sock:/var/run/docker.sock
    - manager-token:/tokens:ro
    - policy:/policy
    command:
    - uvicorn
    - mcp_manager:app
    - --host
    - 0.0.0.0
    - --port
    - '8003'
    networks:
    - mcp
    mem_limit: 384m
    cpus: 0.5
    depends_on:
      init:
        condition: service_completed_successfully
      egress-proxy:
        condition: service_started
  egress-proxy:
    restart: unless-stopped
    read_only: true
    cap_drop: *id001
    security_opt: *id002
    pids_limit: 128
    tmpfs: *id003
    environment:
      MCP_POLICY_FILE: /policy/mcp-policy.json
      LOG_LEVEL: INFO
    volumes:
    - policy:/policy:ro
    networks:
    - execution
    - mcp
    - internet
    mem_limit: 256m
    cpus: 0.5
    depends_on:
      init:
        condition: service_completed_successfully
    image: ghcr.io/taitai2661/mix-agent-egress:v0.2.0
    container_name: mix-agent-egress-proxy-1
networks:
  database:
    internal: true
  execution:
    internal: true
  mcp:
    internal: true
  internet: {}
volumes:
  postgres: null
  keys: null
  db-secret: null
  execution-token: null
  mcp-token: null
  manager-token: null
  data: null
  policy: null
  workspace: null
  packages: null
  mcp-shared: null
  browser-token: null
  browser-provisioner-token: null
  browser-data: null
x-casaos:
  id: io.github.taitai2661.mix-agent
  main: app
  index: /
  port_map: '8080'
  scheme: http
  title:
    en_US: MIX-agent
    ja_JP: MIX-agent
  tagline:
    en_US: Your AI workspace on a trusted home network.
    ja_JP: 家庭内ネットワークで使うAIワークスペース。
  description:
    en_US: Self-hosted chat, agents, tools and MCP. For one administrator on a trusted LAN or VPN; not suitable
      for internet exposure.
    ja_JP: チャット・Agent・Tool・MCPをまとめたセルフホスト環境。信頼できるLAN/VPNの単一管理者向け。インターネット公開は非推奨。
  tips:
    before_install:
      en_US: Uses port 8080 and multiple containers including PostgreSQL, browser and MCP runners. Requires significant
        memory and disk. Install only on a trusted LAN/VPN. MCP management mounts the Docker socket. Back up existing
        MIX-agent data before an upgrade. Set PUBLIC_ORIGIN to your real HTTPS URL for OAuth and web push.
      ja_JP: 8080番ポートとPostgreSQL・Browser・MCPなど複数コンテナを使用します。十分なメモリと空き容量が必要です。信頼できるLAN/VPN内でのみ使用してください。MCP管理用コンテナはDocker
        socketをマウントします。更新前にデータをバックアップし、OAuth・Web Push利用時はPUBLIC_ORIGINに実際のHTTPS URLを設定してください。
  author: MIX-agent contributors
  developer: MIX-agent contributors
  category: AI
  architectures:
  - amd64
  version: 0.2.0
  update_at: '2026-09-23'
  release_notes:
    en_US: First versioned release with expanded agent and provider capabilities.
    ja_JP: Agent・Provider機能を拡充した初回のバージョン付きリリース。
