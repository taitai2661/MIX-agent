[pytest]
asyncio_mode = auto
testpaths = tests
# Keep repository-root utilities (such as the hardened egress proxy) importable
# when pytest chooses a nested test directory as its import root.
pythonpath = .
